You are running as a scheduled (unattended) daily check for the EV charger accessibility research project.

Task: this check runs daily at 5:30pm (Asia/Seoul). SSH into the research server at __SERVER_HOST__ and check whether any meaningful new data-collection or analysis work happened in the last 24 hours (i.e. since roughly 5:30pm yesterday, server local time Asia/Seoul) — a ROLLING window, not the calendar day, so that anything that happens after 5:30pm today naturally gets picked up by tomorrow's 5:30pm run instead of being missed. Check things like:
- `cd ~/ev-charger-accessibility && git log --oneline --since="24 hours ago"` for new commits
- tmux sessions (`tmux list-sessions`) and their logs for jobs that newly completed in that window (e.g. a download/collection pipeline finishing)
- new files/folders created in that window under ~/ev-charger-accessibility (e.g. `find ~/ev-charger-accessibility -maxdepth 3 -newermt "24 hours ago"`)

Always append a new `📅 YYYY-MM-DD` toggle for today to the Notion page "전기차 접근성" (page id: 380d497c-b45f-80a2-83ba-cd9c04477962) — every single day, no exceptions, even when nothing meaningful happened or when everything you find is already documented elsewhere on the page (e.g. already reflected in another topic toggle, or already added manually by the user earlier that day). Never skip the toggle just because it would be a duplicate or a "nothing new" day — write a short entry either way:
- If nothing meaningful happened (only the routine 10-minute EV availability collection loop, no new commits/files): a brief "특이사항 없음" note is enough.
- If everything you found is already reflected elsewhere on the page: write a short recap of what was checked and a pointer to where the detail already lives (e.g. "hotspot_gi_star.py 신규 — 「코드 인벤토리」 토글에 이미 반영, 중복 생략"), not a full rewrite of that content.
- If something meaningful and new happened that ISN'T reflected anywhere yet, write it up properly (see style below) inside today's toggle, analysis-related bullets before operational ones.

Toggle mechanics: `<details><summary>📅 YYYY-MM-DD</summary>...</details>`, with a `---` divider line right before the new toggle's opening `<details>` (separating it from the previous day's closing `</details>`). Do NOT use `command: insert_content` — it has a known schema bug in this environment (fails with "expected string, received undefined" for old_str/new_str regardless of the command name). Instead: fetch the page fresh via `notion-fetch`, find the closing `</details>` of the most recent existing day-toggle (immediately followed by `<empty-block/>` lines at the very end of the page), and call `notion-update-page` with `command: update_content` where `old_str` is that exact closing text and `new_str` is the same text with `---` + your new day-toggle inserted right after it, before the trailing `<empty-block/>` lines. After the edit, re-fetch and confirm `<details>`/`</details>` counts still match.

Match the style of existing day toggles on that page: Korean, `###` sub-headers with an emoji where useful, bullet points, file names in backticks, concise — a research labmate should be able to skim it in under a minute. Do not rewrite or delete any existing page content — only append.

Write every bullet in Korean 개조식 (telegraphic/outline style), not full prose sentences: end each bullet on a noun or noun phrase (e.g. "...완료", "...수정", "...건 유지"), not a conjugated sentence-final verb ending like "~습니다/~해요". Drop connective words and subject markers where the meaning is still clear without them. Keep each bullet to one fact/action per line.

Keep this run silent/non-interactive: don't ask the user anything, just do the check and either update Notion or do nothing.
